---
const pathname = Astro.url.pathname;

// Función helper para saber si el link está activo
const isActive = (path: string) => pathname === path || (path !== '/' && pathname.startsWith(path));
---

<nav class="fixed top-0 left-0 right-0 z-50 flex justify-center pt-6 px-4">
    <div class="bg-slate-900/90 backdrop-blur-xl border border-white/10 rounded-full px-1.5 py-1.5 flex items-center shadow-2xl shadow-cyan-500/10 max-w-sm w-full justify-between">
        
        <a href="/" class={`px-5 py-2.5 rounded-full text-xs font-bold uppercase tracking-widest transition-all duration-300 flex items-center gap-2 ${isActive('/') ? 'bg-cyan-600/20 text-cyan-400 shadow-[0_0_15px_rgba(34,211,238,0.3)] border border-cyan-500/30' : 'text-gray-400 hover:text-white hover:bg-white/5'}`}>
            <span>🏠</span> Inicio
        </a>

        <div class="w-px h-4 bg-white/10"></div>

        <a href="/how-to-play" class={`px-5 py-2.5 rounded-full text-xs font-bold uppercase tracking-widest transition-all duration-300 flex items-center gap-2 ${isActive('/how-to-play') ? 'bg-cyan-600/20 text-cyan-400 shadow-[0_0_15px_rgba(34,211,238,0.3)] border border-cyan-500/30' : 'text-gray-400 hover:text-white hover:bg-white/5'}`}>
            <span>❔</span> Reglas
        </a>

    </div>
</nav>